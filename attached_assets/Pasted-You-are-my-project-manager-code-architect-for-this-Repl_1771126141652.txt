You are my project manager + code architect for this Replit project.

GOAL
Prepare everything needed so that another AI (a code model) can implement a WebGL/Three.js topographic wallpaper that reacts to mouse movement and is fully integrated into my current Legalit site on Replit.

TASK

Inspect my project and answer these questions in detail.

Then prepare a clean technical summary + file map that I can paste into another AI to generate the final code.

STEP 1 – ANALYZE THE CURRENT PROJECT
Please inspect the whole Replit project and answer clearly:

Stack & tooling

Is the project using React + TypeScript + Vite + Tailwind?

Confirm exact versions of React, Vite, and TypeScript from package.json.

List all important UI/animation libraries already installed (Framer Motion, GSAP, Three.js, etc.).

Entry points and layout

Which file is the true application entry? (src/main.tsx or similar)

Which file is the root layout that wraps all pages/sections? (e.g. src/App.tsx or a Layout component)

Where is the main “hero” / top section of the homepage defined?

File structure

List the main folders and files under src/ (components, pages, sections, hooks).

Highlight which files render the homepage content and navigation.

Note where global layout or background styling currently lives (e.g. App.tsx, Layout.tsx, index.css, tailwind.css).

Styling setup

Confirm Tailwind is configured and where the main CSS/Tailwind entry file is.

Note any global background colors, gradients, or images currently applied to body, html, or main wrappers that could conflict with a WebGL background.

Technical constraints

Confirm whether Three.js is already installed.

If not installed, state clearly that it is missing and where to add it in package.json.

Mention anything that might conflict with a full‑screen <canvas> (e.g. full‑screen sections with overflow-hidden, special z‑index systems, etc.).

STEP 2 – PREPARE A TECHNICAL SUMMARY FOR ANOTHER AI
After analysis, produce a concise technical brief that another AI (the “codex”) can use to generate the topographic background code.

The brief must include:

Project overview

Short description of the stack, routing style, and styling system.

Short description of the homepage structure (what main sections/components it has).

Root mounting point

Exact file path and component name where a background component should be mounted so it appears behind everything on all pages (for example: src/App.tsx wrapping the main layout).

Any important wrappers (e.g. <BrowserRouter>, <Layout>) that the background must sit inside or outside of.

Styling constraints

Current background colors/gradients and whether they should be removed or kept when the WebGL background is added.

Any z‑index conventions in Tailwind or custom CSS that the background should respect (e.g. Nav uses z-50, modals use z-40, etc.).

Dependencies

Confirm if three is installed. If not, write the exact command (e.g. npm install three) that should be run.

Mention any other relevant dependencies that might help (e.g. framer-motion already present).

Integration notes

Recommended place to create a new component like src/components/TopographicBackground.tsx.

Any special considerations (SSR, strict mode, layout shifts) that the code generator should know.

OUTPUT FORMAT (VERY IMPORTANT)
Your final answer should have exactly two sections:

PROJECT ANALYSIS

Bullet points answering all questions in STEP 1.

BRIEF FOR CODE AI

A short, clean paragraph for each of the 5 items in STEP 2, written as if you are talking to another code‑generation AI, so I can copy‑paste it directly.

Do not write any code yourself.
Focus only on analysis, paths, file names, and clear explanations.

When you are done, stop. I will copy your BRIEF FOR CODE AI section into another AI to generate the actual WebGL topographic background component.