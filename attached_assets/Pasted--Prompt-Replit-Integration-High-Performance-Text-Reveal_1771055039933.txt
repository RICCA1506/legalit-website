🚀 Prompt: Replit Integration - High Performance Text Reveal

Ruolo: Sei un Senior React Developer specializzato in animazioni 60FPS.Contesto: Stiamo lavorando sul progetto https://legalit.replit.app.

Il Tuo Compito:

Ho un componente React già scritto e ultra-ottimizzato (basato sulla tecnica "CSS Masking & Stagger" di landonorris.com). Voglio che tu prenda questo codice e lo integri nel mio progetto.

Ecco il Componente (Già Ottimizzato):Non modificarne la logica, è già perfetta per performance.



JavaScript



// components/LandoReveal.jsximport React from 'react';export const LandoReveal = ({ text, className = "", delay = 0 }) => {

  // Dividiamo il testo in parole e poi in caratteri per gestire gli spazi

  const words = text.split(" ");



  return (

    <h2 className={`font-bold leading-tight ${className}`} aria-label={text}>

      {words.map((word, wordIndex) => (

        <span key={wordIndex} className="inline-block whitespace-nowrap mr-[0.25em]">

          {word.split("").map((char, charIndex) => {

            // Calcolo indice globale approssimativo per l'effetto onda

            const globalIndex = wordIndex * 5 + charIndex;

            return (

              <span

                key={charIndex}

                className="inline-block overflow-hidden align-bottom" // MASKING (Il segreto di Lando)

              >

                <span

                  className="inline-block translate-y-[-110%] opacity-0 animate-reveal"

                  style={{

                    animationDelay: `${delay + globalIndex * 0.03}s`, // Stagger puro CSS

                    willChange: 'transform',

                  }}

                >

                  {char}

                </span>

              </span>

            );

          })}

        </span>

      ))}

      {/* Definizione Animation Keyframes locale per portabilità */}

      <style jsx>{`

        @keyframes reveal {

          0% { transform: translateY(-110%); opacity: 0; }

          100% { transform: translateY(0); opacity: 1; }

        }

        .animate-reveal {

          animation: reveal 0.8s cubic-bezier(0.16, 1, 0.3, 1) forwards; // Curve 'Quint' per effetto smooth

        }

      `}</style>

    </h2>

  );

};

Istruzioni di Integrazione:



Crea il file components/LandoReveal.jsx con il codice sopra.

Importalo e usalo all'interno del file/sezione che ti indico qui sotto.

Assicurati che il testo passato come prop sia visibile e contrastato rispetto allo sfondo.

DOVE IMPLEMENTARLO:

Voglio che sostituisci il titolo principale o il testo in questa specifica sezione del sito: i sottotitoli dei testi grandi, le tre parole colorate nella home "hero" a cui cambierai colore per matchare quello del logo #003767, intorno a queste tre parole metti una sfocatura in tutte le direzioni delle lettere per far vedere meglio e risaltare il font che ti daro dopo :useremo poi questi due vediamo dove["Agisci come un Senior Frontend Developer. Ho bisogno di configurare nel progetto due font specifici da Google Fonts per replicare un design editoriale misto (Brutalist + High Fashion), ma voglio applicarli manualmente in seguito.

Per favore, esegui solo queste operazioni di setup:



Importazione: Aggiungi nel <head> (o nel CSS tramite @import) i link per Google Fonts per queste due famiglie:

'Inter' con peso 900 (Black).

'Playfair Display' con peso 700 (Bold).

CSS Utility Classes: Crea nel file CSS due classi riutilizzabili con queste specifiche precise:

Classe .text-brutalist (per replicare lo stile "PARTNERS"): usa font-family: 'Inter', font-weight: 900, text-transform: uppercase, e un letter-spacing: -0.04em (per renderlo compatto).

Classe .text-editorial (per replicare lo stile "& CAMPAIGNS"): usa font-family: 'Playfair Display', font-weight: 700, text-transform: uppercase, e un letter-spacing: -0.03em (per l'eleganza).

Variabili Colore: Crea una variabile CSS root per il giallo neon: --color-neon: #DFFF00;.

Non applicare queste classi a nessun tag HTML per ora, voglio solo che il codice sia pronto nel file CSS e nell'index."]